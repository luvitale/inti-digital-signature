image: node:14

services:
  - 4tqrgqe5yrgfd/electron:latest

stages:
  - test
  - deploy

cache:
  paths:
    - node_modules/

test:
  script:
    - npm install
    - npm run test:unit

deploy:
  script:
    - apt update
    - apt -y install rpm
    - npm install
    - npm run electron:build
  artifacts:
    paths:
      - dist_electron/*.AppImage
      - dist_electron/*.snap
      - dist_electron/*.deb
      - dist_electron/*.rpm
