image: node:14

cache:
  paths:
    - node_modules/

build:
  image: 4tqrgqe5yrgfd/electron:latest
  script:
    - npm install
    - npm run electron:build
  artifacts:
    paths:
      - dist_electron/*.AppImage
      - dist_electron/*.snap
      - dist_electron/*.deb
